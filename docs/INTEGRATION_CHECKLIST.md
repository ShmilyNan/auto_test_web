# 前后端联调检查清单

## 准备阶段

### 环境准备

- [ ] 后端服务已启动
- [ ] 后端服务地址确认（如：http://localhost:8899
- [ ] 数据库已初始化
- [ ] 测试数据已准备
- [ ] Swagger/OpenAPI 文档已生成
- [ ] 前端环境变量已配置（.env.local）
- [ ] 前端服务已启动（pnpm dev）

### 工具准备

- [ ] Postman 已安装并配置
- [ ] 浏览器 DevTools 已熟悉
- [ ] API 测试脚本已准备

---

## 认证模块

### 用户登录

- [ ] POST /api/auth/login
  - [ ] 正确账号密码登录成功
  - [ ] 错误账号密码返回 400
  - [ ] 返回 Token 格式正确
  - [ ] 返回用户信息正确

### Token 验证

- [ ] GET /api/auth/me
  - [ ] 携带有效 Token 返回用户信息
  - [ ] 未携带 Token 返回 401
  - [ ] Token 过期返回 401

### Token 刷新

- [ ] POST /api/auth/refresh
  - [ ] 有效刷新 Token 返回新 Token
  - [ ] 无效刷新 Token 返回 401

### 用户登出

- [ ] POST /api/auth/logout
  - [ ] 登出成功
  - [ ] Token 失效

---

## 项目管理模块

### 获取项目列表

- [ ] GET /api/projects
  - [ ] 返回项目列表
  - [ ] 分页参数生效
  - [ ] 搜索功能正常
  - [ ] 排序功能正常

### 获取单个项目

- [ ] GET /api/projects/:id
  - [ ] 返回正确项目信息
  - [ ] 项目不存在返回 404

### 创建项目

- [ ] POST /api/projects
  - [ ] 创建成功返回 201
  - [ ] 参数验证失败返回 400
  - [ ] 权限不足返回 403

### 更新项目

- [ ] PUT /api/projects/:id
  - [ ] 更新成功返回 200
  - [ ] 参数验证失败返回 400
  - [ ] 项目不存在返回 404
  - [ ] 权限不足返回 403

### 删除项目

- [ ] DELETE /api/projects/:id
  - [ ] 删除成功返回 200
  - [ ] 项目不存在返回 404
  - [ ] 权限不足返回 403

### 项目统计

- [ ] GET /api/projects/:id/stats
  - [ ] 返回项目统计信息
  - [ ] 包含用例数量、执行次数等

---

## 测试用例模块

### 获取用例列表

- [ ] GET /api/test-cases
  - [ ] 返回用例列表
  - [ ] 分页参数生效
  - [ ] 支持按项目筛选

### 获取单个用例

- [ ] GET /api/test-cases/:id
  - [ ] 返回正确用例信息
  - [ ] 用例不存在返回 404

### 创建用例（手工添加）

- [ ] POST /api/test-cases
  - [ ] 创建成功返回 201
  - [ ] 支持多种 HTTP 方法（GET、POST、PUT、DELETE等）
  - [ ] Headers 处理正确
  - [ ] Body 参数处理正确
  - [ ] 参数验证失败返回 400

### 更新用例

- [ ] PUT /api/test-cases/:id
  - [ ] 更新成功返回 200
  - [ ] 权限不足返回 403

### 删除用例

- [ ] DELETE /api/test-cases/:id
  - [ ] 删除成功返回 200
  - [ ] 权限不足返回 403

### 从文件导入用例

- [ ] POST /api/projects/:id/import/file
  - [ ] 支持 YAML 格式
  - [ ] 支持 JSON 格式
  - [ ] 文件解析错误返回 400
  - [ ] 批量创建成功

### 从 Swagger 导入用例

- [ ] POST /api/projects/:id/import/swagger
  - [ ] 通过 URL 导入
  - [ ] 通过文件上传导入
  - [ ] 正确解析 Swagger 规范
  - [ ] 提取所有 paths 和 schemas
  - [ ] 支持选择部分接口导入

### 从 cURL 导入用例

- [ ] POST /api/projects/:id/import/curl
  - [ ] 正确解析 cURL 命令
  - [ ] 提取 URL、方法、Headers、Body
  - [ ] 转换为测试用例格式

### 执行单个用例

- [ ] POST /api/test-cases/:id/execute
  - [ ] 执行成功返回结果
  - [ ] 包含响应状态、响应体、耗时
  - [ ] 期望断言验证正确

### 批量执行用例

- [ ] POST /api/test-cases/execute
  - [ ] 批量执行成功
  - [ ] 返回执行 ID
  - [ ] 支持异步执行

---

## 测试报告模块

### 获取执行记录列表

- [ ] GET /api/executions
  - [ ] 返回执行记录列表
  - [ ] 分页参数生效
  - [ ] 支持按项目筛选

### 获取执行详情

- [ ] GET /api/executions/:id
  - [ ] 返回执行统计信息
  - [ ] 包含总用例数、通过数、失败数、通过率
  - [ ] 包含开始时间、结束时间、耗时

### 获取执行结果（用例级别）

- [ ] GET /api/executions/:id/results
  - [ ] 返回所有用例执行结果
  - [ ] 包含每个用例的状态（通过/失败）
  - [ ] 失败用例包含错误信息
  - [ ] 支持分页

### 导出报告

- [ ] GET /api/executions/:id/export
  - [ ] 支持 HTML 格式导出
  - [ ] 支持 PDF 格式导出
  - [ ] 支持 JSON 格式导出
  - [ ] 报告包含完整执行信息
  - [ ] 报告包含图表（通过率、趋势等）

---

## 权限控制模块

### 角色权限

- [ ] Admin 角色：拥有所有权限
- [ ] Project Manager 角色：拥有项目管理权限
- [ ] Test Engineer 角色：拥有用例管理权限
- [ ] Viewer 角色：只有查看权限

### 权限控制

- [ ] 无权限操作返回 403
- [ ] 前端根据权限隐藏/显示按钮
- [ ] 前端根据权限隐藏/显示菜单
- [ ] 未登录访问返回 401

---

## WebSocket 实时推送

### 连接测试

- [ ] WebSocket 连接成功
- [ ] 携带 Token 验证
- [ ] 连接断开自动重连

### 消息推送

- [ ] 用例执行进度实时推送
- [ ] 执行完成通知
- [ ] 连接异常处理

---

## 异常处理

### 参数验证

- [ ] 必填参数缺失返回 400
- [ ] 参数类型错误返回 400
- [ ] 参数格式错误返回 400

### 资源不存在

- [ ] 项目不存在返回 404
- [ ] 用例不存在返回 404
- [ ] 执行记录不存在返回 404

### 权限错误

- [ ] 未登录返回 401
- [ ] 无权限返回 403

### 服务器错误

- [ ] 服务器内部错误返回 500
- [ ] 错误信息记录日志

### 网络错误

- [ ] 前端网络错误提示
- [ ] 请求超时处理
- [ ] 重试机制

---

## 性能测试

### 响应时间

- [ ] 列表接口响应时间 < 500ms
- [ ] 详情接口响应时间 < 300ms
- [ ] 创建接口响应时间 < 500ms

### 数据量测试

- [ ] 1000+ 项目列表加载正常
- [ ] 10000+ 用例列表加载正常（分页）
- [ ] 批量导入 100+ 用例成功

### 并发测试

- [ ] 支持多个请求同时执行
- [ ] 批量执行 100+ 用例成功

---

## 兼容性测试

### 浏览器兼容性

- [ ] Chrome 最新版
- [ ] Firefox 最新版
- [ ] Edge 最新版
- [ ] Safari 最新版

---

## 文档完整性

- [ ] API 文档完整（Swagger/OpenAPI）
- [ ] 前端类型定义完整
- [ ] 错误码文档完整
- [ ] 联调文档更新

---

## 浏览器控制台测试命令

```javascript
// 测试 API 连通性
await ApiTestHelper.testConnection();

// 检查 Token
ApiTestHelper.checkToken();

// 打印环境配置
ApiTestHelper.printEnvConfig();

// 测试项目列表
fetch("/api/projects", {
  headers: {
    Authorization: "Bearer " + localStorage.getItem("token"),
  },
})
  .then((r) => r.json())
  .then(console.log);
```

---

## 联调记录

| 日期       | 模块 | 测试接口        | 结果 | 问题 | 解决方案 | 负责人 |
| ---------- | ---- | --------------- | ---- | ---- | -------- | ------ |
| 2024-03-07 | 认证 | /api/auth/login | ✅   | -    | -        | -      |
| 2024-03-07 | 项目 | /api/projects   | ✅   | -    | -        | -      |
| ...        | ...  | ...             | ...  | ...  | ...      | ...    |

---

## 备注

- 所有测试通过后，标记联调完成
- 未通过的接口需要记录问题和解决方案
- 后续迭代需要重新联调相关接口
